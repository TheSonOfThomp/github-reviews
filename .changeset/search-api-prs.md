---
"chrome-extension": minor
---

Fetch pull requests through the GitHub Search API so PRs in large repos (1,500+ open PRs) and review requests sent to your teams now show up. Failed fetches, inaccessible repos, and rate limits are now reported for each repo instead of showing an empty list (#10).
