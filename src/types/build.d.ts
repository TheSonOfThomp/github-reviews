// Injected at build time by @rollup/plugin-replace (see rollup.config.mjs):
// the short commit hash of the checkout the bundle was built from, or ""
// for builds from main.
declare const __BUILD_COMMIT__: string;
